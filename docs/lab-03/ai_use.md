# Lab 3 — AI Use and Reflection 

**LLM/agent used:** Gemini 3.8 Flash (High)

## Selected key prompts (6–10)
| # | Prompt (summarised) | What I did with the result |
| --- | --- | --- |
| 1 | Everything works and passes the test. I want to add one more detail that shouldn't affect the tests or go against the specifications. Let's add a voluntary Change Password option for the authenticated users as well. Turn the user tab at the navbar into a small dropdown, inside the dropdown should display the users full email as an added element and then a button to change passwords. Be sure not to change the rest of the files. | I wanted to add a voluntary change password option for users and specified how it should be to the agent. The agent was able to do this successfully without affecting any of the other features. |
| 2 | The Ticket Operations tab slides up to the middle of the navbar when scrolling down. I like the sticky tab option when scrolling, so let's fix it by having it a few pixels under the navbar when scrolling, but should snap back to its original position if we scroll up again. | A section was a sticky tab and I fixed it while keeping the function because I liked it. |
| 3 | Review the current branch against the contract and specifications. Report back any inconsistencies. | I check the progress against the constract and specs before pushing. |
| 4 | I'm unable to see the tickets as an admin or IT staff. The admin and staff should be able to see all tickets, please fix it so that they can see it. | I caught an error where the admin and staff were unable to see the tickets, I had to guide it through fixing it. |
| 5 | Make me a short list about the things I should be checking myself in this feature. | I wanted to make sure I wasn't missing anything when checking the work so I had it list out every new feature it implemented. |
| 6 | [After pasting an error from the terminal] Why is the file giving errors when the page works fine? | The terminal was still throwing errors so I had to fix it. |

## Reflection

This time around things went smoothly and basically repeated the steps from the last lab. There wasn't much more I did than last time.